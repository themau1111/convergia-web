import Link from "next/link";
import { revalidatePath } from "next/cache";

import {
  createLocalPocClient, createLocalPocPortfolio, deactivateLocalPocClient,
  getDataSources, getLocalPocClients, getPortfolios, startLocalPocTestCall,
  startManualTestCall, startPortfolioTestCalls, syncLocalPocPortfolios,
  type LocalClientRecord, updateLocalPocClient,
} from "@/lib/control-api";

const DEFAULT_AGENT = {
  agent_name: "Valeria", company_name: "la institución",
  personality: "Empática, clara y directa. Explica antes de solicitar un compromiso y nunca inventa información.",
};
const value = (data: FormData, key: string) => String(data.get(key) ?? "").trim();
const required = (data: FormData, key: string) => { const result = value(data, key); if (!result) throw new Error(`missing_${key}`); return result; };
const numeric = (data: FormData, key: string) => Number(value(data, key) || 0);
const agentFrom = (data: FormData) => ({
  agent_name: value(data, "agent_name") || DEFAULT_AGENT.agent_name,
  company_name: value(data, "company_name") || DEFAULT_AGENT.company_name,
  personality: value(data, "personality") || DEFAULT_AGENT.personality,
  ...(value(data, "voice_id") ? { voice_id: value(data, "voice_id") } : {}),
});

export default async function LocalOperationsPage() {
  const [sources, portfolios] = await Promise.all([getDataSources(), getPortfolios()]);
  const localSources = sources.filter((source) => source.adapter_type === "local_poc");
  const sourceIds = new Set(localSources.map((source) => source.id));
  const localPortfolios = portfolios.filter((portfolio) => sourceIds.has(portfolio.data_source_id));
  const entries: Array<readonly [string, { clients: LocalClientRecord[]; error: boolean }]> = await Promise.all(localPortfolios.map(async (portfolio) => {
    try { return [portfolio.id, { clients: await getLocalPocClients(portfolio.id), error: false }] as const; }
    catch { return [portfolio.id, { clients: [] as LocalClientRecord[], error: true }] as const; }
  }));
  const results = new Map(entries);
  const total = entries.reduce((sum, [, item]) => sum + item.clients.length, 0);

  async function syncLocal(data: FormData) { "use server"; await syncLocalPocPortfolios(required(data, "data_source_id")); revalidatePath("/app/settings/catalogs"); }
  async function addPortfolio(data: FormData) { "use server"; await createLocalPocPortfolio(required(data, "data_source_id"), { name: required(data, "name"), ...(value(data, "key") ? { key: value(data, "key") } : {}), description: value(data, "description") }); revalidatePath("/app/settings/catalogs"); }
  async function addClient(data: FormData) { "use server"; await createLocalPocClient(required(data, "portfolio_id"), { nombre_cliente: required(data, "nombre_cliente"), telefono: required(data, "telefono"), dia_pago: value(data, "dia_pago"), saldo_pendiente: numeric(data, "saldo_pendiente"), articulo: value(data, "articulo"), pagos_atrasados: numeric(data, "pagos_atrasados"), modalidad: value(data, "modalidad"), cuota_semanal: numeric(data, "cuota_semanal") }); revalidatePath("/app/settings/catalogs"); }
  async function editClient(data: FormData) { "use server"; await updateLocalPocClient(required(data, "portfolio_id"), numeric(data, "client_id"), { nombre_cliente: required(data, "nombre_cliente"), telefono: required(data, "telefono"), dia_pago: value(data, "dia_pago"), saldo_pendiente: numeric(data, "saldo_pendiente"), articulo: value(data, "articulo"), pagos_atrasados: numeric(data, "pagos_atrasados"), modalidad: value(data, "modalidad"), cuota_semanal: numeric(data, "cuota_semanal") }); revalidatePath("/app/settings/catalogs"); }
  async function deactivateClient(data: FormData) { "use server"; await deactivateLocalPocClient(required(data, "portfolio_id"), numeric(data, "client_id")); revalidatePath("/app/settings/catalogs"); }
  async function callClient(data: FormData) { "use server"; if (value(data, "confirmed") !== "yes") throw new Error("confirmation_required"); await startLocalPocTestCall(required(data, "portfolio_id"), numeric(data, "client_id")); }
  async function callManual(data: FormData) { "use server"; if (value(data, "confirmed") !== "yes") throw new Error("confirmation_required"); await startManualTestCall({ nombre_cliente: value(data, "nombre_cliente"), telefono: required(data, "telefono"), dia_pago: value(data, "dia_pago"), saldo_pendiente: numeric(data, "saldo_pendiente"), articulo: value(data, "articulo"), pagos_atrasados: numeric(data, "pagos_atrasados"), modalidad: value(data, "modalidad"), cuota_semanal: numeric(data, "cuota_semanal"), agent: agentFrom(data) }); }
  async function callPortfolio(data: FormData) { "use server"; if (value(data, "confirmed") !== "yes") throw new Error("confirmation_required"); await startPortfolioTestCalls(required(data, "portfolio_id"), required(data, "confirmation_name"), agentFrom(data)); }

  return <main className="members-shell local-ops-page">
    <header className="members-header"><div><p className="eyebrow">Laboratorio operativo</p><h1>Pruebas y carteras</h1><p className="muted">Consulta destinatarios locales, administra datos ficticios y ejecuta llamadas controladas.</p></div><Link href="/">← Volver al pulso</Link></header>
    <section className="local-ops-summary"><article><small>Carteras locales</small><strong>{localPortfolios.length}</strong></article><article><small>Destinatarios activos</small><strong>{total}</strong></article><article><small>Origen</small><strong>SQLite POC</strong></article></section>

    <section className="manual-call-card"><div><p className="eyebrow">Llamada rápida</p><h2>Llama sin registrar a la persona</h2><p className="muted">El número y sus datos se usan sólo para esta llamada; no se agregan a ninguna cartera.</p></div><form action={callManual}><div className="form-grid"><label>Teléfono<input name="telefono" required placeholder="8112345678" /></label><label>Nombre opcional<input name="nombre_cliente" /></label></div><details><summary>Agregar datos de cobranza y personalidad</summary><div className="form-grid advanced-call-fields"><label>Empresa<input name="company_name" placeholder={DEFAULT_AGENT.company_name} /></label><label>Agente<input name="agent_name" placeholder={DEFAULT_AGENT.agent_name} /></label><label className="wide-field">Personalidad<textarea name="personality" placeholder={DEFAULT_AGENT.personality} /></label><label>Saldo<input name="saldo_pendiente" type="number" min="0" step=".01" /></label><label>Producto<input name="articulo" /></label><label>Día de pago<input name="dia_pago" /></label><label>Pagos atrasados<input name="pagos_atrasados" type="number" min="0" /></label><label>Modalidad<input name="modalidad" /></label><label>Cuota<input name="cuota_semanal" type="number" min="0" step=".01" /></label><label>ID de voz<input name="voice_id" /></label></div></details><div className="call-submit-row"><label className="call-confirm"><input type="checkbox" name="confirmed" value="yes" required /> Confirmo una llamada real</label><button className="test-call-button">Iniciar llamada ↗</button></div></form></section>

    <section className="portfolio-browser"><div className="section-heading"><div><p className="eyebrow">Destinatarios registrados</p><h2>Carteras locales</h2></div><span>{total} personas disponibles</span></div><div className="portfolio-stack">{localPortfolios.map((portfolio) => { const result = results.get(portfolio.id); const count = result?.clients.length ?? 0; return <details className="portfolio-panel" key={portfolio.id}><summary><span><strong>{portfolio.name}</strong><small>{portfolio.external_key}</small></span><b>{result?.error ? "Error al cargar" : `${count} activos`}</b></summary>{result?.error ? <p className="portfolio-load-error">No se pudo consultar esta cartera.</p> : <><div className="portfolio-recipient-scroll">{result?.clients.map((client) => <article className="recipient-row" key={client.id}><div><strong>{client.nombre_cliente}</strong><a href={`tel:${client.telefono}`}>{client.telefono}</a><small>{client.articulo || "Sin producto"} · ${client.saldo_pendiente.toLocaleString("es-MX")}</small></div><div className="recipient-actions"><form action={callClient}><input type="hidden" name="portfolio_id" value={portfolio.id} /><input type="hidden" name="client_id" value={client.id} /><label className="call-confirm"><input type="checkbox" name="confirmed" value="yes" required /> Confirmar</label><button className="test-call-button">Llamar</button></form><details><summary>Editar</summary><form action={editClient} className="inline-client-editor"><input type="hidden" name="portfolio_id" value={portfolio.id} /><input type="hidden" name="client_id" value={client.id} /><input name="nombre_cliente" defaultValue={client.nombre_cliente} required /><input name="telefono" defaultValue={client.telefono} required /><input name="saldo_pendiente" type="number" min="0" step=".01" defaultValue={client.saldo_pendiente} /><input name="articulo" defaultValue={client.articulo ?? ""} /><input name="dia_pago" defaultValue={client.dia_pago ?? ""} /><input name="pagos_atrasados" type="number" min="0" defaultValue={client.pagos_atrasados} /><input name="modalidad" defaultValue={client.modalidad ?? ""} /><input name="cuota_semanal" type="number" min="0" step=".01" defaultValue={client.cuota_semanal} /><button className="text-button">Guardar</button></form><form action={deactivateClient}><input type="hidden" name="portfolio_id" value={portfolio.id} /><input type="hidden" name="client_id" value={client.id} /><button className="danger-text">Desactivar</button></form></details></div></article>)}</div><form action={callPortfolio} className="portfolio-batch-call"><div><strong>Llamar a toda la cartera</strong><small>{count ? `Escribe “${portfolio.name}” para confirmar ${count} llamadas reales.` : "Agrega destinatarios antes de iniciar un lote."}</small></div><input type="hidden" name="portfolio_id" value={portfolio.id} /><input name="confirmation_name" required disabled={!count} placeholder={portfolio.name} /><input type="hidden" name="confirmed" value="yes" /><button className="danger-action" disabled={!count}>Llamar a todos</button></form></>}</details>; })}</div></section>

    <section className="local-admin-card"><div><p className="eyebrow">Administración local</p><h2>Agregar datos de prueba</h2><p className="muted">Estas altas sí permanecen en SQLite.</p>{localSources.map((source) => <form action={syncLocal} key={source.id}><input type="hidden" name="data_source_id" value={source.id} /><button className="text-button">Sincronizar carteras existentes</button></form>)}</div><div className="local-admin-forms"><details><summary>Nueva cartera</summary><form action={addPortfolio}>{localSources.length === 1 ? <input type="hidden" name="data_source_id" value={localSources[0].id} /> : <label>Fuente<select name="data_source_id" required>{localSources.map((source) => <option value={source.id} key={source.id}>{source.name}</option>)}</select></label>}<label>Nombre<input name="name" required /></label><label>Clave opcional<input name="key" pattern="[a-z0-9_]+" /></label><label>Descripción<input name="description" /></label><button className="primary-action">Crear cartera</button></form></details><details><summary>Nuevo destinatario</summary><form action={addClient}><label>Cartera<select name="portfolio_id" required>{localPortfolios.map((portfolio) => <option value={portfolio.id} key={portfolio.id}>{portfolio.name}</option>)}</select></label><div className="form-grid"><label>Nombre<input name="nombre_cliente" required /></label><label>Teléfono<input name="telefono" required /></label><label>Saldo<input name="saldo_pendiente" type="number" min="0" step=".01" /></label><label>Producto<input name="articulo" /></label><label>Día de pago<input name="dia_pago" /></label><label>Pagos atrasados<input name="pagos_atrasados" type="number" min="0" /></label><label>Modalidad<input name="modalidad" /></label><label>Cuota<input name="cuota_semanal" type="number" min="0" step=".01" /></label></div><button className="primary-action">Agregar destinatario</button></form></details></div></section>
  </main>;
}
